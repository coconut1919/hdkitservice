apiVersion: v1
kind: Service
metadata:
  name: hdkitservice-svc
  namespace: backend
spec:
  type: ClusterIP
  selector:
    app: hdkitservice
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
